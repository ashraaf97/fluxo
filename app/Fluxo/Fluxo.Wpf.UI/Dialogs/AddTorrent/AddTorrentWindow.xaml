<Window x:Class="Fluxo.Wpf.UI.Dialogs.AddTorrent.AddTorrentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Fluxo.Wpf.UI.Dialogs.AddTorrent"
        mc:Ignorable="d"
        Title="{StaticResource TOR_TITLE}" Height="230" Width="620" ShowInTaskbar="True"
        WindowStartupLocation="CenterScreen" Closing="Window_Closing">
    <DockPanel Margin="15">
        <Grid DockPanel.Dock="Bottom" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="LblStatus" Grid.Column="0" Margin="0,14,10,0"
                       TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
            <Button x:Name="BtnOK" Content="{StaticResource MSG_OK}" Grid.Column="1"
                    Margin="0,10,5,0" Padding="15,3" Click="BtnOK_Click"/>
            <Button x:Name="BtnCancel" Content="{StaticResource ND_CANCEL}" Grid.Column="2"
                    Margin="5,10,0,0" Padding="5,3" Click="BtnCancel_Click"/>
        </Grid>

        <StackPanel DockPanel.Dock="Top">
            <TextBlock Text="{StaticResource TOR_MAGNET_LABEL}" Margin="0,0,0,6"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="TxtUrl" Grid.Column="0" Margin="0,0,5,0" Padding="3"
                         TextWrapping="NoWrap" AcceptsReturn="False"
                         VerticalContentAlignment="Center"/>
                <Button x:Name="BtnBrowse" Grid.Column="1" Content="{StaticResource TOR_BROWSE}"
                        Padding="12,3" Click="BtnBrowse_Click"/>
            </Grid>
            <ProgressBar x:Name="PrgBusy" Height="4" Margin="0,12,0,0"
                         IsIndeterminate="True" Visibility="Collapsed"/>
        </StackPanel>
    </DockPanel>
</Window>
