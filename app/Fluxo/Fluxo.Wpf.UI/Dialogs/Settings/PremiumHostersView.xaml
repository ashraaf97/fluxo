<UserControl x:Class="Fluxo.Wpf.UI.Dialogs.Settings.PremiumHostersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Fluxo.Wpf.UI.Dialogs.Settings"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <TextBlock Padding="10" Text="{StaticResource SETTINGS_PREMIUM_HOSTERS}" FontSize="14" DockPanel.Dock="Top" />
        <ScrollViewer>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Margin="10,5,10,10"
                           TextWrapping="Wrap" Text="{StaticResource MSG_PREMIUM_HOSTERS_INTRO}"/>

                <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Margin="10,10,10,0" Text="{StaticResource LBL_ALLDEBRID_API_KEY}"/>
                <TextBox Name="TxtAllDebridApiKey" Grid.Row="2" Grid.ColumnSpan="2" Margin="10" VerticalContentAlignment="Center"
                         AcceptsReturn="False" TextWrapping="NoWrap"/>

                <TextBlock Grid.Row="3" Grid.ColumnSpan="2" Margin="10,10,10,0" Text="{StaticResource LBL_REALDEBRID_API_KEY}"/>
                <TextBox Name="TxtRealDebridApiKey" Grid.Row="4" Grid.ColumnSpan="2" Margin="10" VerticalContentAlignment="Center"
                         AcceptsReturn="False" TextWrapping="NoWrap"/>

                <TextBlock Grid.Row="5" Grid.ColumnSpan="2" Margin="10,10,10,0" Text="{StaticResource LBL_DEBRID_ORDER}"/>
                <TextBlock Grid.Row="6" Grid.ColumnSpan="2" Margin="10,3,10,0" Opacity="0.75"
                           TextWrapping="Wrap" Text="{StaticResource MSG_DEBRID_ORDER_HINT}"/>
                <ListBox Name="LstProviderOrder" Grid.Row="7" Grid.Column="0" Margin="10" Height="80"
                         SelectionChanged="LstProviderOrder_SelectionChanged"/>
                <StackPanel Grid.Row="7" Grid.Column="1" Margin="0,10,10,10" VerticalAlignment="Top">
                    <Button Name="BtnMoveUp" Padding="10,3" Content="{StaticResource Q_MOVE_UP}" Click="BtnMoveUp_Click"/>
                    <Button Name="BtnMoveDown" Padding="10,3" Margin="0,5,0,0" Content="{StaticResource Q_MOVE_DN}" Click="BtnMoveDown_Click"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </DockPanel>
</UserControl>
