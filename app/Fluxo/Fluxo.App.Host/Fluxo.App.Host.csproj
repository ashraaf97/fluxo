<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <!--<TargetFrameworks>net4.6.0;net6.0</TargetFrameworks>-->
    <PublishTrimmed>true</PublishTrimmed>
    <TrimMode>Link</TrimMode>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <!--<Platforms>x86</Platforms>-->
    <Platforms>x86;x64</Platforms>
    <AssemblyName>fluxo-app-host</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="13.0.4" />
  </ItemGroup>

  <ItemGroup>
    <None Update="fluxoff.native_host.json">
      <CopyToOutputDirectory>Always</CopyToOutputDirectory>
    </None>
    <None Update="fluxo_chrome.native_host.json">
      <CopyToOutputDirectory>Always</CopyToOutputDirectory>
    </None>
  </ItemGroup>

  <Import Project="..\NativeMessaging\NativeMessaging.projitems" Label="Shared" />

  <Import Project="..\Fluxo.Messaging\Fluxo.Messaging.projitems" Label="Shared" />

  <PropertyGroup Condition="'$(Configuration)'=='Release'">
    <DebugType>none</DebugType>
    <DebugSymbols>false</DebugSymbols>
  </PropertyGroup>

</Project>
