Opens the browser's extension page and then removes itself, so the installer can
walk the user to the page where they enable the Fluxo integration extension.

Loaded with: chrome.exe --load-extension=PATH_TO_EXT

Vendored from https://github.com/subhra74/xdm-helper-chrome (GPLv3) so the
installer build does not depend on that repository staying available.
