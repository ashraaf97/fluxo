1. Create a directory named Fluxo.App.Host at the same level as fluxo-app executable
2. Copy binary of Fluxo.App.Host to that directory
3. Linux only - create a file named source_pkg at the same level as fluxo-app executable and set its content to deb/rpm/tar.xz for Ubuntu/Fedora/Arch
4. Linux only - copy fluxo-logo.svg to the same level as fluxo-app executable